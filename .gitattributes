# Cluster shell scripts must keep LF endings: a CR byte makes bash fail with
# "$'\r': command not found" on the Slurm nodes, even when the file is edited on Windows.
*.sbatch text eol=lf
*.sh     text eol=lf

# Windows-side launchers stay CRLF.
*.ps1    text eol=crlf

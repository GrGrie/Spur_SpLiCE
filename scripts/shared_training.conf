# Shared settings for the CRP training entry point.
# Edit the mode-specific config for graph/factor settings.

MODEL="${MODEL:-resnet18_large}"
SEED="${SEED:-${SLURM_ARRAY_TASK_ID:-0}}"

# Main SSL/training settings.
EPOCHS="${EPOCHS:-500}"
BATCH_SIZE="${BATCH_SIZE:-256}"
LEARNING_RATE="${LEARNING_RATE:-0.01}"
SPLICE_WEIGHT="${SPLICE_WEIGHT:-0.01}"
SIMCLR_WEIGHT="${SIMCLR_WEIGHT:-1.0}"

# Shared relational-training schedule.
CRP_TEMPERATURE="${CRP_TEMPERATURE:-0.25}"
CRP_START_EPOCH="${CRP_START_EPOCH:-10}"
CRP_WARMUP_EPOCHS="${CRP_WARMUP_EPOCHS:-10}"
CRP_DECAY_START_EPOCH="${CRP_DECAY_START_EPOCH:-200}"
CRP_DECAY_END_EPOCH="${CRP_DECAY_END_EPOCH:-350}"

# Graph/cache preparation.
CRP_AUTO_BUILD_GRAPH="${CRP_AUTO_BUILD_GRAPH:-true}"
CRP_FORCE_REBUILD="${CRP_FORCE_REBUILD:-false}"
CRP_CACHE_BATCH_SIZE="${CRP_CACHE_BATCH_SIZE:-64}"
CRP_CACHE_NUM_WORKERS="${CRP_CACHE_NUM_WORKERS:-4}"
COBALT="${COBALT:-false}"
COBALT_CONCEPTS_PATH="${COBALT_CONCEPTS_PATH:-}"
CRP_SPATIAL_BALANCE="${CRP_SPATIAL_BALANCE:-false}"
CRP_SPATIAL_BALANCE_VARIANT="${CRP_SPATIAL_BALANCE_VARIANT:-}"
CRP_SPATIAL_BALANCE_PATH="${CRP_SPATIAL_BALANCE_PATH:-}"
CRP_SPATIAL_BALANCE_FLOOR="${CRP_SPATIAL_BALANCE_FLOOR:-0.25}"
CRP_SPATIAL_FREQUENCY_POWER="${CRP_SPATIAL_FREQUENCY_POWER:-0.0}"
RELATIONAL_GRAPH_VARIANT="${RELATIONAL_GRAPH_VARIANT:-}"

# Shared optimizer/augmentation settings.
OPTIMIZER="${OPTIMIZER:-SGD}"
WEIGHT_DECAY="${WEIGHT_DECAY:-1e-4}"
MOMENTUM="${MOMENTUM:-0.9}"
SIMCLR_TEMPERATURE="${SIMCLR_TEMPERATURE:-0.05}"
SSL_CROP_MIN="${SSL_CROP_MIN:-0.2}"
COSINE="${COSINE:-false}"
LR_WARMUP="${LR_WARMUP:-false}"
HEAD="${HEAD:-mlp}"
FEAT_DIM="${FEAT_DIM:-128}"
TRIAL="${TRIAL:-0}"
LR_DECAY_EPOCHS="${LR_DECAY_EPOCHS:-auto}"
LR_DECAY_RATE="${LR_DECAY_RATE:-0.1}"

# SAM-only settings.
SAM_BASE_OPTIMIZER="${SAM_BASE_OPTIMIZER:-SGD}"
SAM_RHO="${SAM_RHO:-0.05}"
SAM_NO_GRAD_NORM="${SAM_NO_GRAD_NORM:-false}"
ONLY_SAM_STEP_SIZE="${ONLY_SAM_STEP_SIZE:-false}"

# Frozen SpLiCE/OpenCLIP backend.
SPLICE_MODEL="${SPLICE_MODEL:-open_clip:ViT-B-32}"
SPLICE_PRETRAINED="${SPLICE_PRETRAINED:-laion2b_s34b_b79k}"
SPLICE_VOCAB="${SPLICE_VOCAB:-openimages_v7}"
SPLICE_VOCAB_SIZE="${SPLICE_VOCAB_SIZE:--1}"
SPLICE_L1_PENALTY="${SPLICE_L1_PENALTY:-0.25}"
SPLICE_BATCH_SIZE="${SPLICE_BATCH_SIZE:-128}"
SPLICE_NUM_WORKERS="${SPLICE_NUM_WORKERS:-1}"

# Legacy SpLiCE modes remain available for historical controls.
SPLICE_CONCEPTS="${SPLICE_CONCEPTS:-}"
SPLICE_SCORE_REDUCTION="${SPLICE_SCORE_REDUCTION:-mean}"
SPLICE_AUTO_TOP_K="${SPLICE_AUTO_TOP_K:-10}"
SPLICE_AUTO_RANKING_METHOD="${SPLICE_AUTO_RANKING_METHOD:-utility}"
SPLICE_AUTO_SPLIT="${SPLICE_AUTO_SPLIT:-train}"
SPLICE_AUTO_PROBE_C="${SPLICE_AUTO_PROBE_C:-1.0}"
SPLICE_AUTO_PROBE_MAX_ITER="${SPLICE_AUTO_PROBE_MAX_ITER:-1000}"
SPLICE_AUTO_PROBE_CV_FOLDS="${SPLICE_AUTO_PROBE_CV_FOLDS:-5}"
SPLICE_AUTO_UTILITY_MAX_SAMPLES="${SPLICE_AUTO_UTILITY_MAX_SAMPLES:-10000}"
SPLICE_AUTO_UTILITY_CANDIDATE_POOL="${SPLICE_AUTO_UTILITY_CANDIDATE_POOL:-100}"
SPLICE_AUTO_UTILITY_MIN_REPAIR="${SPLICE_AUTO_UTILITY_MIN_REPAIR:-0.0}"
SPLICE_AUTO_UTILITY_MIN_MARGINAL="${SPLICE_AUTO_UTILITY_MIN_MARGINAL:-0.0}"
SPLICE_AUTO_MIN_MEAN_WEIGHT="${SPLICE_AUTO_MIN_MEAN_WEIGHT:-0.0}"
SPLICE_AUTO_LABEL_PENALTY="${SPLICE_AUTO_LABEL_PENALTY:-0.0}"
SPLICE_AUTO_INSTABILITY_PENALTY="${SPLICE_AUTO_INSTABILITY_PENALTY:-0.0}"
SPLICE_AUTO_REQUIRE_CONSISTENT_DIRECTION="${SPLICE_AUTO_REQUIRE_CONSISTENT_DIRECTION:-false}"
SPLICE_AUTO_DEDUPLICATE_CONCEPTS="${SPLICE_AUTO_DEDUPLICATE_CONCEPTS:-true}"
SPLICE_AUTO_USE_ABS_SCORE="${SPLICE_AUTO_USE_ABS_SCORE:-false}"
SPLICE_CONDITIONAL_ON_TARGET="${SPLICE_CONDITIONAL_ON_TARGET:-false}"
SPLICE_SCORE_THRESHOLD="${SPLICE_SCORE_THRESHOLD:-0.0}"
SPLICE_SCORE_QUANTILE="${SPLICE_SCORE_QUANTILE:-0.95}"
SPLICE_ROUTING_MODE="${SPLICE_ROUTING_MODE:-none}"
SPLICE_STRONG_CROP="${SPLICE_STRONG_CROP:-false}"
SPLICE_STRONG_COLOR_JITTER="${SPLICE_STRONG_COLOR_JITTER:-false}"
SPLICE_STRONG_COLOR_JITTER_P="${SPLICE_STRONG_COLOR_JITTER_P:-0.8}"
SPLICE_STRONG_GRAYSCALE_P="${SPLICE_STRONG_GRAYSCALE_P:-0.2}"
SPLICE_STRONG_BLUR_P="${SPLICE_STRONG_BLUR_P:-0.5}"
SPLICE_STRONG_BLUR_SIGMA="${SPLICE_STRONG_BLUR_SIGMA:-0.1}"
SPLICE_STRONG_LINE_RECOLOR="${SPLICE_STRONG_LINE_RECOLOR:-false}"
SPLICE_STRONG_BLUR_KERNEL_SIZE="${SPLICE_STRONG_BLUR_KERNEL_SIZE:-}"

# Evaluation/checkpoint settings.
LINEAR_TRAIN_SPLIT="${LINEAR_TRAIN_SPLIT:-ds_train}"
LINEAR_EVAL_SPLIT="${LINEAR_EVAL_SPLIT:-val}"
LINEAR_PROBE_MODE="${LINEAR_PROBE_MODE:-periodic}"
LINEAR_PROBE_FREQ="${LINEAR_PROBE_FREQ:-25}"
LINEAR_PROBE_EPOCHS="${LINEAR_PROBE_EPOCHS:-100}"
LINEAR_PROBE_SOLVER="${LINEAR_PROBE_SOLVER:-logistic}"
LINEAR_PROBE_L2="${LINEAR_PROBE_L2:-0.001}"
LINEAR_PROBE_TOLERANCE="${LINEAR_PROBE_TOLERANCE:-0.000001}"
LINEAR_PROBE_MAX_EPOCHS="${LINEAR_PROBE_MAX_EPOCHS:-200}"
LINEAR_LEARNING_RATE="${LINEAR_LEARNING_RATE:-1.0}"
LINEAR_LR_DECAY_EPOCHS="${LINEAR_LR_DECAY_EPOCHS:-auto}"
LINEAR_LR_DECAY_RATE="${LINEAR_LR_DECAY_RATE:-0.2}"
LINEAR_WEIGHT_DECAY="${LINEAR_WEIGHT_DECAY:-0.0}"
LINEAR_SPURIOUS_PROBE="${LINEAR_SPURIOUS_PROBE:-true}"
FINAL_TEST="${FINAL_TEST:-false}"
SAVE_FREQ="${SAVE_FREQ:-25}"
KEEP_CHECKPOINTS="${KEEP_CHECKPOINTS:-true}"
CHECKPOINT_KEEP_COUNT="${CHECKPOINT_KEEP_COUNT:-4}"
DELETE_CHECKPOINTS_AFTER_TRAINING="${DELETE_CHECKPOINTS_AFTER_TRAINING:-true}"
PRINT_FREQ="${PRINT_FREQ:-25}"
RANK_EVAL_FREQ="${RANK_EVAL_FREQ:-0}"

# Cluster/runtime paths.
PROJECT_DIR="${PROJECT_DIR:-/home/xar68reb/Spur_SpLiCE}"
CONDA_ENV="${CONDA_ENV:-grgrie-train}"
DATA_FOLDER="${DATA_FOLDER:-/home/xar68reb/Datasets}"
CHECKPOINT_DIR="${CHECKPOINT_DIR:-outputs/training}"
RESUME="${RESUME:-}"
SPLICE_SCORE_CACHE_DIR="${SPLICE_SCORE_CACHE_DIR:-outputs/splice_score_cache}"
NUM_WORKERS="${NUM_WORKERS:-5}"
AMP="${AMP:-true}"
CHANNELS_LAST="${CHANNELS_LAST:-true}"
CUDNN_ENABLED="${CUDNN_ENABLED:-true}"
CUDNN_BENCHMARK="${CUDNN_BENCHMARK:-false}"
PREPARE_ONLY="${PREPARE_ONLY:-false}"

# W&B is mandatory for real SSL training.
USE_WANDB="${USE_WANDB:-true}"
WANDB_PROJECT="${WANDB_PROJECT:-Spur_SpLiCE}"
WANDB_ENTITY="${WANDB_ENTITY:-gsgrechkin-rptu}"
WANDB_EXTRA_TAGS="${WANDB_EXTRA_TAGS:-machine_cluster}"
